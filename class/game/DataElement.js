// All elements in the DOM that are associated with a piece of data and update with it can be represented as a DataElement.
// A DataElement 

// Represents a piece of data that is:
// - associated with an element in the DOM
// - whose style updates according to the data
class DataElement {
    generateElem() {};
    get elem() {};
    set elem(elem) {};
    updateElem() {};
    resetElem() {};
}